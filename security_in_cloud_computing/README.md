In order to get nice import add path to this directory to your PYTHONPATH.

For example something like this in ```~/.bashrc```:

```export PYTHONPATH="${PYTHONPATH}:/home/gabriel/white_rose/system_security_2"```

